using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DodgeMode : GameMode
{

    /* public DodgeMode(GameModeData gameModeData, Spawner spawnController, Difficulty difficulty) : base(gameModeData, spawnController, difficulty) {}
    public override IEnumerator Run(Action incrementLevel){
        while(true) {
            yield return new WaitForSeconds(3.0f); //1/UnityEngine.Random.Range(spawnRateMin, spawnRateMax));
            // Target targetRef = this.targets[Random.Range(0, this.targets.Length)];
            // targetSpawner.Spawn(targetRef, pointGenerator(), Quaternion.identity);
            // float speed = initialSpeed * (1 + (level * levelSpeedMultiplier)); //calculated as percent increase
            // target.Init(Vector3.forward * speed, () => {Debug.Log("HIT");}); //change callback, maybe record data orrr do that somewhere else
        }
    } */
}

