using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class SimpleHitMode : GameMode
{

    /* public SimpleHitMode(GameModeData gameModeData, Spawner spawnController, Difficulty difficulty) : base(gameModeData, spawnController, difficulty) {}

    public override IEnumerator Run(Action incrementLevel){
        Debug.Log("Next Round");
        while(true) {
             yield return new WaitForSeconds(3.0f); //1/UnityEngine.Random.Range(spawnRateMin, spawnRateMax));
            Target targetRef = this.data.targets[UnityEngine.Random.Range(0, this.data.targets.Length)];
            //float targetSpeed = this.data.initialSpeed * (GameController.Instance.Level * this.levelMultipliers.speed);
            //spawnController.Spawn(targetRef, Vector3.zero, Quaternion.identity, targetSpeed, incrementLevel);
        }
    } */

}
