using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NeuroMonstersMode : GameMode
{
/* 
    public NeuroMonstersMode(GameModeData gameModeData, Spawner spawnController, Difficulty difficulty) : base(gameModeData, spawnController, difficulty) {}
    public override IEnumerator Run(Action incrementLevel){
        yield return new WaitForSeconds(3.0f);
    } */
}
